<div class="page-header">
  <h1>Assign <%= @engine.serial %> to Stock </h1>
</div>

<div class="content">
  <div class="paragraph">
    <%= render 'engine' %>
  </div>

  <%= form_for @engine, :url => {:action => 'process_engine'} do |f| %>
    <% if @engine.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@engine.errors.count, "error") %> prohibited this engine from being saved:</h2>

        <ul>
        <% @engine.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <h3>Stock Serial to Assign to</h3>
    <div class="field">
      <strong>Stock Serial: </strong>
      <%= f.text_field :stock_id %>
    </div>
    <div class="actions">
      <%= f.submit class: "btn btn-sm btn-success" %>
    </div>
  <% end %>
</div>
